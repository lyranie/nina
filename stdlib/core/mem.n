mod std.core.mem;

void* copy(void* dst, void* src, int len) {
    char* d = dst as char*;
    char* s = src as char*;

    int i = 0;

    while (i < len) {
        d[i] = s[i++];
    }

    ret dst;
}
