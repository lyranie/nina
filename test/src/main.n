mod test.main;

use libc;

type Foo(int* i);

Foo* foo() {
    Foo* foo = libc.malloc(Foo.$size);
    foo.i = libc.malloc(int.$size);

    ret foo;
}

int main() {
    Foo* foo = foo();
    *foo.i = 69;

    libc.printf("%d\n", *foo.i);

    ret 0;
}
