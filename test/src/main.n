mod test.main;

use std.core.stub;
use std.io;

int main(int argc, char** argv) {
    String[] args = stub.argv_to_args(argc, argv);

    int i = 0;
    while (i < argc) {
        io.printn(args[i++]);
    }

    ret 0;
}
