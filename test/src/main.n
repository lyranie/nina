mod test.main;

use std.core.allocator.arena;
use std.core.string;
use std.io.file;
use std.io;

use libc;

type Person(str name, int age);

int main() {
    Person p = new Person(string.create("Nathaniel Pinkerton"), -2);
    io.printn(p);

    ret 0;
}
