mod test.main;

use std.core.string;
use std.io.file;
use std.io;

int main() {
    file.write_text("hello.txt", "Penis");
    io.print(string.create("\e[91m"));
    io.print(file.read_text("hello.txt"));
    io.printn(string.create("\e[0m"));

    ret 0;
}
